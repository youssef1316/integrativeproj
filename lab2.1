#include <iostream>
using namespace std;

int q1(int x) {
    if ( x < 0) return x * -1;
    return x;
}
bool q2(int grades[4]) {
    int x =  (grades[0] + grades[1] + grades[2] + grades[3]) / 4;
    if (x >= 70) return 1;
    return 0;
}
int q3(int val[1]) {
    cout << "The swapped values are: "<< val[1] <<", "<< val[0] << "\n";
}
void q3a(int val[1]) {
    cout << "The modified values are: "<< val[0] * 3 <<", "<< val[1] * 3 << "\n";
}
void q3b (int (&val)[1]) {
    cout << "The modified after passin by refrence values are: "<< val[0] * 2 << ", "<< val[1] * 2 << "\n";
}
int main() {
    /*cout << "Enter a number: \n";
    int x; cin>> x;
    cout << "The absolute value is: "<< q1(x) << "\n"; */

    /*int grades[4];
    for (int i = 0; i < 4; i++) {
        cout << "Enter grade: \n";
        cin>> grades[i];
    }
    if (q2(grades)) {
        cout << "Congratulations you have passed!\n";
    }
    else cout << "you have failed!\n";*/

    /*int val[1];
    cout << "Enter value 1: \n";
    cin>> val[0];
    cout << "Enter value 2: \n";
    cin>>val[1];
    q3(val); */

    /*int val[1];
    cout << "Enter value 1: \n";
    cin>> val[0];
    cout << "Enter value 2: \n";
    cin>> val[1];
    q3a(val);
    q3b(val); */

    int arr[9];
    int min = INT16_MAX;
    int max = INT64_MIN;
    for (int i = 0; i < 10; i++) {
        cout << "Enter the "<< i + 1 << " value: \n";
        cin >> arr[i];
        if (arr[i] > max) max = arr[i];
        if (arr[i] < min) min = arr[i];
    }
    cout << "The max value is: "<< max<<"\n";
    cout << "The min value is: "<< min<<"\n";
}